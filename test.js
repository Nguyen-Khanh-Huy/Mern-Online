import { rejects } from "assert";


// lay du lieu database1=> IO
// lay du lieu database1=> IO
// lay du lieu database1=> IO
// lay du lieu database1=> IO
// xu ly
// in ra manhinh

// Callback Hell
// 
Promise

function layDatabase() {
    return new Promise(function (resolve, reject) {
        if (ketquaoke) {
            resolve(ketquaoke);
        } else {
            reject(err)
        }
    });
}

layDatabase()
    .then(data => {

    })
    .catch(err => {

    })

    // Xem video async
    // Tim hieu es6 : arrow funcion
    // map js
